package Controls;

public class DataDisplayCanvas extends ResizableCanvas{
    //The DataDisplayCanvas Displays and manages a list of strokes.
    //It is bound to a controller, which contains and manages the list.
}
